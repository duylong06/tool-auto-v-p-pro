/**
 * Memory - lưu lịch sử task và ngữ cảnh hội thoại.
 *
 * GIAI ĐOẠN HIỆN TẠI: chưa implement (agent chạy xong là mất lịch sử).
 * TODO (giai đoạn 2): kết nối PostgreSQL, lưu mỗi lần runAgent() vào bảng `tasks`
 * gồm: user_message, tools_used, final_answer, created_at.
 */

export {};
